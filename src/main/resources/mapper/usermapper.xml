<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hana.app.repository.UserRepository">
    <insert id="insert" parameterType="userEntity">
        INSERT INTO user VALUES (0, #{id},#{name},#{type},#{classroomId})
    </insert>
    <delete id="delete" parameterType="Integer">
        DELETE FROM user WHERE id=#{id}
    </delete>
    <update id="update" parameterType="userEntity">
        UPDATE user SET name=#{name}, type=#{type} WHERE id=#{id}
    </update>
    <select id="select" resultType="userEntity">
        SELECT * FROM user
    </select>
    <select id="selectOne" parameterType="Integer" resultType="userEntity">
        SELECT * FROM user WHERE id=#{id}
    </select>
</mapper>